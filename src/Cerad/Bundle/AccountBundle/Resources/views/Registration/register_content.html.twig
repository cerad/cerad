<div class="instructions"><h3>Register New Account</h3></div>

<form 
    class="fos_user_registration_register" style="width: 500px;"
    action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" >
<fieldset>
    {{ form_row(form.name)  }}
    {{ form_row(form.email) }}
    {{ form_row(form.plainPassword.first)  }}
    {{ form_row(form.plainPassword.second) }}
    {{ form_rest(form) }}
</fieldset>
<fieldset>
    <div class="controls">
        <input type="submit" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" />
    </div>
</fieldset>
</form>
<h4>Unique Email Instructions</h4>
<p style="width: 600px;">If you have an ArbiterSports account (either referee or contact) 
    then your unique email should be your <em>Arbiter</em> email.  
    Using your arbiter email will allow the sytem to tie you directly to your games.
</p>
    
